import DashboardSkeleton from '@/app/ui/skeletons';
 
export default function Loading() {
  return <DashboardSkeleton />;
}

// the parentheses around 'overview' makes it so a new route isn't created.